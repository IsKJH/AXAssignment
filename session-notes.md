# Session Notes — 2026-06-11 (발표 D-1, Figma 전수 재검증 라운드)

## 현재 상태
- 전 화면 Figma 1:1 재검증(에이전트 6종 병렬) → 발견된 명세 불일치 **6건 수정 완료 + 기기 검증**
- 상세 규칙/구조는 **CLAUDE.md가 최신** — 먼저 읽을 것
- 워킹 트리에 미커밋 변경 있음 (아래 수정분) — 커밋은 사용자 승인 대기

## 이번 라운드 수정 (Figma 명세 근거)
1. **정기 거래 매월 자동 연장** (16:172) — 앱 시작 시 topUpRecurringSeries()가
   살아있는 시리즈를 현재 월+11까지 보충. 최신 인스턴스가 과거면 중단된 시리즈로 보고 스킵
2. **이후 내역 모두 삭제 → 과거 인스턴스 일반화** (7:149) — deleteSeriesFrom 후 clearSeriesFlags
3. **날짜 입력 제한 해제** (16:172) — 드럼롤 오늘 ±1년, 저장 시 속한 주기 자동 분류 (기기 검증)
   ※ 회귀 발견·수정: 기본값 클램프 LaunchedEffect가 카테고리 왕복 시 재실행돼
   주기 밖 선택 날짜를 덮어씀 → rememberSaveable 1회 가드
4. **카테고리 선택 모드 재탭 해제** (17:178) — 재탭 시 해제+화면 유지, 뒤로가기 → 미분류(0L).
   TransactionEvent/DetailEvent.SetCategory nullable화, Add VM은 첫 로드만 기본 카테고리 채움
5. **통계 지난달 데이터 없음 → "0원" 회색** (488:1046) — prevAmount=0이면 증가 빨강 금지
6. 홈 정렬 타이브레이커 `date DESC, id DESC` ("최신 등록 순")
7. **수입 카테고리 미사용 정리** (명세 공백 의도적 결정, 사용자 승인) — 홈 "수입"+초록 도트,
   상세 "수입" 고정, 수입 탭 카테고리 행 숨김, 저장 시 null 강제, INCOME 시드 제거.
   부수 효과: 탭 전환 시 카테고리 보존(명세 16:172 "기입력 값 유지" 충족)

## 의도적 미수정 (검토 후 결정)
- 카테고리 7개 초과: 버튼 비활성 대신 토스트 안내 (기능 제한은 이중 가드로 동일)
- 카테고리 시트 스와이프 다운 닫기 미지원 (외부 탭/뒤로가기만 — 제스처 회귀 위험)
- SCR-04 이후/전체 수정 시 날짜 미전파 — 각 인스턴스가 자기 월 유지(명세 모호, 의도적 설계)
- 6개월 평균 라벨 "6개월 평균 소비 금액"(명세 측) vs 디자인 "평균 소비 금액", 칩 패딩 2dp
- 기존 의도적 결정 유효: 키보드 자동 포커스 없음, % 보정 없음, 6개월 내역 현재 주기 고정,
  초과 문구 "수입의 N원 초과하셨습니다"(446:731)

## 기기 검증 결과 (Z Flip3)
- 통계 0원 회색 / 5월 날짜 입력→5월 주기 분류→삭제 / 재탭 해제→미분류 / 앱 시작 top-up 무해(중복 없음)
- 미검증: top-up 양성 케이스(월 전환 시뮬 불가, 로직 검토로 갈음), 이후 삭제 일반화(데이터 변형 회피)

## 남은 작업 (코드 아님)
1. **커밋 승인 + git push** — 명시 요청 시에만
2. 시연 데이터 입력 + 잔여 테스트 거래 정리, `_trash/` 수동 삭제

## 작업 팁
- Figma: get_design_context/get_screenshot(base64), SVG 실측이 수치 정답
- mobile-mcp 규칙(좌표 환산·요소 리스트 1순위·스와이프 방향·키보드 BACK)은
  CLAUDE.md + verify-screen-on-device 스킬에 정리됨 (2026-06-11 웹 조사 반영)
- **devicekit APK 설치됨** → type_keys로 한글 직접 입력 가능 — 시연 데이터 자동 입력 OK
- **고속 입력**: `python -X utf8 scripts/device_input.py add <금액> <메모> [카테고리]` — ~13초/건,
  bulk JSON 지원. 기반은 범용 드라이버 `scripts/ui.py`(좌표 0, 셀렉터+게이트) — 새 화면은 플로우만 추가
- gradle.properties에 configuration-cache 활성화 — 무변경 installDebug 20초→7초
- 다음 단계 후보(발표 후): python-uiautomator2(상주 데몬, dump 2.3초→ms), Roborazzi(기기 없는 스크린샷 검증)
