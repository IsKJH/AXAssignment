# Session Notes — 2026-06-11 (발표 D-1, 시연 준비 완료)

## 현재 상태
- **전 화면 완성 + 3-그룹 전수 QA 통과** — 미커밋 코드 없음(아래 수정 커밋됨), origin 푸시는 미실행
- **시연 데이터 무결성 확인** — 카테고리 원상(기본7+카페+구독), 월별 지출 시드 원본 일치, 6월 885,800
- 상세 규칙/구조/인셋/기기 규칙은 **CLAUDE.md가 최신** — 먼저 읽을 것

## 최종 QA에서 수정한 것 (2026-06-11)
- 카테고리 선택 하이라이트 리셋 버그(remember 키 제거) / 정기 체크박스 라벨 탭 불가
- 시작일 변경 시 활성 홈·통계 현재 주기 스냅 / 과거·미래 연도 주기 라벨에 연도 prefix
- 홈 리스트 끝 FAB 회피 여백
- **데이터 사건**: QA 중 카페 카테고리가 삭제·재생성(id 18→21)되며 FK SET_NULL로
  카페 거래 9건이 미분류화 → id 21로 재연결 복구. 삭제→미분류는 명세대로(앱 버그 아님)
- **WAL 함정**: `cat budget_db`만 pull하면 WAL 미반영 stale 상태를 읽음 →
  반드시 db+wal+shm 함께 pull 후 checkpoint해야 실제 상태 확인됨

## 시연 데이터 (기기에 시드됨)
- 방법: DB 직접 시드 (`_workspace/seed/seed_demo.py` → pull/시드/push)
  - device_input.py는 날짜 지정 불가(전부 오늘 날짜) → 통계 시연이 빈약해져 DB 시드 채택
  - Git Bash에서 adb 기기 경로는 `MSYS_NO_PATHCONV=1` 필수 (경로 변환 오류)
- 내용: 2026-01~06 거래 114건. 정기 3시리즈(월급 350만/월세 50만/유튜브 프리미엄),
  월별 일반 지출 10건 내외, 미분류 1건(축의금), 추가 수입 1건(중고 거래)
- 카테고리: 기본 7개 + 커스텀 카페(현재 id 21)/구독(19). 구버전 INCOME·기타 커스텀은 정리함
- 검증 완료: 홈(25% 사용중)/통계(주거 56%, -14만원 파랑, 0원 회색)/6개월 내역(평균 104.9만)
  /topUp 자동연장(3시리즈 모두 2027-05까지)
- **재시드**: 앱 force-stop → `python -X utf8 _workspace/seed/seed_demo.py` → push 절차 반복

## 의도적 결정 (스펙과 다르거나 해석)
- 수입은 카테고리 미사용 / 키보드 자동 포커스 없음 / % 보정 없음 /
  6개월 내역 현재 주기 고정 / 초과 문구 446:731 기준 / 7개 초과 토스트 안내
- SCR-04 이후·전체 수정 시 날짜 미전파 (각 인스턴스가 자기 월 유지 — 명세 모호)
- 시연 월급은 6/25 미래 날짜 — 정기 거래 사전 등록 기능 어필 포인트

## 작업 팁
- 기기 자동화 규칙(좌표/스와이프/한글/폴더블 스크린샷)은 verify-screen-on-device 스킬 참조
- 토큰 절약: 작업 단위로 /clear 권장
